# Dependencies
node_modules/

# Build output — `npm run build` regenerates dist/index.html
dist/

# Claude Code working docs: CLAUDE.md, the spec, the task list, launch.json
.claude/

# Personal reference notes — how things work and why, for explaining this
# project to someone else. Not written for a public audience; never pushed.
notes/

# Editor and OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local env / tooling caches
*.local
.env
.env.*
!.env.example
.vite/
coverage/
*.tsbuildinfo
npm-debug.log*
# Corpus input for `--audit`, which takes a directory at runtime and stores
# nothing. These are real sequences and must never enter the repo; the two
# hand-checked fixtures under fixtures/ are anonymised and are the only
# sequence files that are tracked.
test_xmls/
